/**
 * Default action names that match the standard Apps Script template.
 * Override any of these via the `actions` option in GoogleSheetsClient.
 *
 * @type {{ getAll: string, add: string, update: string }}
 */
export const DEFAULT_ACTIONS = {
  getAll: 'getAll',
  add:    'agregar',
  update: 'actualizar',
};
